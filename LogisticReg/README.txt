To run the code extract the zip file into a folder named LR. The code is in a folder named LR. 

In your current directory,
outside of the folder where the code is, you can run the code with javac LR/file_name.java and then see the output in terminal.

Then java LR.file_name.